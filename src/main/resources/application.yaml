spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cantina
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: mySuperSecretKeyMySuperSecretKey123456
  expiration: 86400000

kind: Application
